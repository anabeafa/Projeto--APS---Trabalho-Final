@model IEnumerable<ProjetoAna.Models.Produto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.Preco)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
              <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
              <td>
                @Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Nome">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Nome">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Nome">Delete</a>
                <a asp-action="Edit" asp-route-id="@item.Descricao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Descricao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Descricao">Delete</a>
                <a asp-action="Edit" asp-route-id="@item.Quantidade">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Quantidade">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Quantidade">Delete</a>
                <a asp-action="Edit" asp-route-id="@item.Preco">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Preco">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Preco">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
